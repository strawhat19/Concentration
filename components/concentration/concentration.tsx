export default function Concentration(props) {
    return <>
        <div title={props.title} style={{width: `100%`, height: `1080px`, margin: `20px auto`}}>
            Concentration
        </div>
    </>
}